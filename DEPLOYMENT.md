# Claude Cockpit UI - 部署指南

本指南適用於將專案移到另一台終端機執行的情況。

## 📦 專案打包

將整個 `Claude Cockpit` 資料夾複製到目標終端機。

## 🔧 環境準備（在目標終端機執行）

### 必要環境

1. **Node.js** >= 16.x
2. **npm** >= 8.x
3. **Git for Windows**（包含 Git Bash）
4. **Windows 編譯工具**

### 安裝步驟

#### 1. 安裝 Windows 編譯工具（需要管理員權限）

```powershell
npm install --global windows-build-tools
```

> ⚠️ 這個步驟可能需要 10-15 分鐘，請耐心等待。

#### 2. 確認 Git Bash 路徑

檢查 Git Bash 是否安裝在標準路徑：

```powershell
Test-Path "C:\Program Files\Git\bin\bash.exe"
```

如果路徑不同，請修改 `config.json` 中的 `backend.bashPath`。

#### 3. 安裝後端依賴

```bash
cd backend
npm install
```

預期安裝的套件：
- `node-pty@^0.10.1`
- `socket.io@^4.6.0`
- `nodemon@^3.0.1`

#### 4. 安裝前端依賴

```bash
cd frontend
npm install
```

預期安裝的套件：
- React、React-DOM
- Vite、TypeScript
- Tailwind CSS、PostCSS、Autoprefixer
- Xterm.js、Socket.io-client

## 🚀 啟動專案

### 方法一：使用兩個終端機視窗

**終端機 1 - 後端**
```bash
cd backend
npm start
```

**終端機 2 - 前端**
```bash
cd frontend
npm run dev
```

### 方法二：使用啟動腳本（推薦）

我已經建立了啟動腳本，請參考 `start.ps1`（Windows PowerShell）。

## 🌐 訪問應用程式

前端啟動後，開啟瀏覽器訪問：
```
http://localhost:5173
```

## 🔍 故障排除

### node-pty 編譯失敗

**症狀**：`npm install` 時出現編譯錯誤

**解決方案**：
1. 確保已安裝 `windows-build-tools`
2. 使用 Node.js LTS 版本（建議 18.x 或 20.x）
3. 以管理員權限執行安裝指令
4. 如果仍然失敗，嘗試：
   ```bash
   npm install --global node-gyp
   npm install node-pty --build-from-source
   ```

### WebSocket 連線失敗

**症狀**：前端無法連接到後端

**解決方案**：
1. 檢查後端是否正常啟動（應顯示 "Server running on port 3001"）
2. 檢查防火牆設定，允許 localhost:3001
3. 確認 port 3001 未被其他程式佔用：
   ```powershell
   netstat -ano | findstr :3001
   ```

### Git Bash 路徑錯誤

**症狀**：後端啟動後無法建立終端機

**解決方案**：
修改 `config.json` 中的路徑：
```json
{
  "backend": {
    "bashPath": "您的實際 Git Bash 路徑"
  }
}
```

常見路徑：
- `C:\Program Files\Git\bin\bash.exe`
- `C:\Program Files (x86)\Git\bin\bash.exe`
- `D:\Git\bin\bash.exe`

## 📝 配置說明

### config.json

```json
{
  "backend": {
    "port": 3001,              // 後端伺服器 port
    "bashPath": "...",         // Git Bash 路徑
    "terminal": {
      "cols": 80,              // 終端機寬度
      "rows": 30               // 終端機高度
    }
  },
  "frontend": {
    "port": 5173,              // 前端開發伺服器 port
    "socketUrl": "http://localhost:3001"
  }
}
```

## 🎯 驗證清單

啟動後請確認：

- [ ] 後端伺服器成功啟動（顯示 "Server running on port 3001"）
- [ ] 前端開發伺服器成功啟動（顯示 Vite 啟動訊息）
- [ ] 瀏覽器可以訪問 http://localhost:5173
- [ ] 頁面顯示五個角色卡片（PM、RD、UI、TEST、SEC）
- [ ] 終端機區域正常顯示
- [ ] 可以在終端機輸入指令（如 `ls`、`pwd`）
- [ ] 終端機輸出正常顯示
- [ ] WebSocket 連線狀態正常（檢查瀏覽器控制台）

## 🔄 開發模式

如果需要修改程式碼：

**後端**（使用 nodemon 自動重啟）
```bash
cd backend
npm run dev
```

**前端**（Vite 支援熱模組替換）
```bash
cd frontend
npm run dev
```

修改程式碼後會自動更新，無需手動重啟。

## 📦 生產環境建置

如果需要建置生產版本：

```bash
# 建置前端
cd frontend
npm run build

# 輸出位置：frontend/dist/
```

## 🎨 自訂配置

### 修改配色

編輯 `frontend/tailwind.config.js`：

```javascript
colors: {
  slate: {
    950: '#020617',  // 背景色
    900: '#0f172a',  // 卡片背景
    // ...
  },
  emerald: {
    400: '#34d399',  // 主色調
  },
}
```

### 修改角色

編輯 `config.json` 中的 `roles` 陣列。

### 修改狀態關鍵字

編輯 `config.json` 中的 `statusKeywords`，用於自動偵測 Claude Code 的執行狀態。

## 📚 相關文件

- [實作計畫](docs/implementation_plan.md)
- [開發指南](Claude_Cockpit_Dev_Guide.md)
- [專案說明](README.md)

---

如有問題，請參考 `implementation_plan.md` 中的「風險與應對策略」章節。
