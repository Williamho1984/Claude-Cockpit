# Claude Cockpit UI - 快速開始指南

## 🚀 一鍵啟動

### Windows PowerShell

```powershell
.\start.ps1
```

### Git Bash / WSL

```bash
chmod +x start.sh
./start.sh
```

## 📋 手動啟動

### 1. 安裝依賴

**後端**
```bash
cd backend
npm install
```

**前端**
```bash
cd frontend
npm install
```

### 2. 啟動伺服器

**後端**（終端機 1）
```bash
cd backend
npm start
```

**前端**（終端機 2）
```bash
cd frontend
npm run dev
```

### 3. 訪問應用程式

開啟瀏覽器訪問：`http://localhost:5173`

## 📝 詳細文件

- [部署指南](DEPLOYMENT.md) - 完整的環境設定與故障排除
- [實作計畫](docs/implementation_plan.md) - 技術架構與設計決策
- [開發指南](Claude_Cockpit_Dev_Guide.md) - 原始需求與技術規格
- [專案說明](README.md) - 專案概述與功能介紹

## ⚙️ 配置

編輯 `config.json` 自訂設定：
- 伺服器 port
- Git Bash 路徑
- 終端機大小
- 角色定義
- 狀態關鍵字

## 🔍 驗證清單

- [ ] 後端啟動成功（顯示 "Server running on port 3001"）
- [ ] 前端啟動成功（顯示 Vite 啟動訊息）
- [ ] 瀏覽器可訪問 http://localhost:5173
- [ ] 終端機顯示連線狀態為 "Connected"
- [ ] 可以在終端機輸入指令並看到輸出

## 💡 提示

- 使用 `start.ps1` 或 `start.sh` 可自動檢查並安裝依賴
- 修改程式碼後會自動熱更新（前端）
- 後端使用 `npm run dev` 可啟用自動重啟

## ❓ 常見問題

**Q: node-pty 安裝失敗？**  
A: 需要安裝 `windows-build-tools`，請參考 [DEPLOYMENT.md](DEPLOYMENT.md)

**Q: WebSocket 連線失敗？**  
A: 確認後端已啟動且 port 3001 未被佔用

**Q: Git Bash 路徑錯誤？**  
A: 修改 `config.json` 中的 `backend.bashPath`

詳細故障排除請參考 [DEPLOYMENT.md](DEPLOYMENT.md)
